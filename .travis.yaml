language: python
python:
    - "3.4"
    
# command to install dependencies
install: 
    - "pip install -r requirements.txt -r hangupsbot/requirements.txt"
    - "pip install -r test-requirements.txt"

# command to run tests
script: notests
